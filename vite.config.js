import { defineConfig } from 'vite';

export default defineConfig({
    // Base URL for the production build (optional, only needed if your site is hosted on a subdirectory)
    base: '/unterwasser/',
});